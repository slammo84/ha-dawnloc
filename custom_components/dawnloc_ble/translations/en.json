{
  "config": {
    "step": {"user": {"title": "DAWNLoc BLE Bridge", "description": "Send scanner-specific Bluetooth observations to the local DAWNLoc app through MQTT.", "data": {"topic": "MQTT topic"}}},
    "abort": {"already_configured": "DAWNLoc BLE Bridge is already configured."}
  }
}
